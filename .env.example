# LightTranslator Environment Configuration
#
# There is nothing to configure here for normal use. Translation providers are
# configured entirely in the app's Settings UI, and their API keys are stored in
# the WebView's localStorage (unencrypted).
#
# This file is kept so `scripts/check-secrets.js` has a documented home for any
# future build-time variable. Do not put real API keys in it.
