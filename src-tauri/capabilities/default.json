{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for LightTranslator. The webview only needs core event/window plumbing: clipboard, shortcuts, dialogs, autostart and the tray are driven from the Rust side, which the ACL does not gate.",
  "windows": ["main", "quick"],
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-hide",
    "core:window:allow-start-dragging",
    "core:window:allow-start-resize-dragging"
  ]
}
